{
  "name": "dflow-lisbon",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ingest-kalshi": "npx tsx scripts/ingest-markets.ts",
    "sync": "npx tsx scripts/ingest-jupiter.ts && cd server && source venv/bin/activate && python3 generate_embeddings.py"
  }
}