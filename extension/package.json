{
  "name": "predict-markets-extension",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "esbuild": "^0.21.0"
  }
}
